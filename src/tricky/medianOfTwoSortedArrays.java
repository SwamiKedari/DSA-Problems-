package tricky;

public class medianOfTwoSortedArrays {
    public static void main(String[] swami){

    }

    static double medianOfArrays(int n, int m, int a[], int b[])
    {
        // Your Code Here
        // we are given the two arrays with different sizes, we have to find the median of the two arrays using the binary search
        // we have to use a program which is very similar to the binary search program, this can be done by comparing the elements that we get during each iterations in the bs and then moving the index according to it
        return -1;
    }
}
